## Installation

#### Install Dependencies

```
$ composer install
```

```
$ npm install && npm run dev
```


#### Configure the Environment
Create `.env` file:
```
$ cat .env.example > .env
```
Run `php artisan key:generate`